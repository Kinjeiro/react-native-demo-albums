query queryAlbumById($albumId: ID!) {
    album(id: $albumId) {
        id
        title
        photos {
            data {
                id
                title
                url
                thumbnailUrl
            }
        }
    }
}
