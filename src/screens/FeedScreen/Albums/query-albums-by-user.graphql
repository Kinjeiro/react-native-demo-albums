query selectAlbumsByUser($userId: ID!) {
    user(id: $userId) {
        albums(options: { paginate: { page: 0, limit: 3 } }) {
            data {
                id
                title
            }
        }
    }
}
